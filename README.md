# Server

Install postgres, docker and docker compose. 

Create a postgres user or use default user and chenger the values in each api.js with your values, as well as the docker-compose.yml file. 

Cd into each directory and run npm install. 

Finally run docker-compose up to test the endpoints.
