# Server

Install postgres, docker and docker compose. 

Create a postgres user or use default user and change the values in each api.js with your values, as well as the docker-compose.yml file. 

Cd into each directory and run npm install. 

Finally run docker-compose up to test the endpoints i.e localhost:5000/products

